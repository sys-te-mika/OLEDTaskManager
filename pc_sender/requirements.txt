# Core — always required
psutil>=5.9.0
pyserial>=3.5

# Tray launcher (run_tray.pyw) — install to run in background
pystray>=0.19.0
Pillow>=10.0.0

# Optional — NVIDIA GPU usage & temperature
# GPUtil>=1.4.0

# Optional — CPU/GPU temperatures on Windows
# Requires LibreHardwareMonitor running as Administrator
# wmi>=1.5.1
